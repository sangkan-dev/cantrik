# Demo skill (installed via `cantrik skill install demo`)

This file is a sample skill snippet. After `cantrik skill install demo`, it appears under `.cantrik/skills/demo-tip.md` and may be selected for LLM context when relevant to the user message.

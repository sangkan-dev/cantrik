# Used by `cross build` (release workflow: aarch64-unknown-linux-gnu).
# Host-side `apt install protobuf-compiler` does not reach the cross container; lance-encoding needs `protoc`.
[target.aarch64-unknown-linux-gnu]
pre-build = ["apt-get update && apt-get install -y protobuf-compiler"]

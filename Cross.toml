# Optional: local `cross build --target aarch64-unknown-linux-gnu` from an x86_64 machine.
# Release CI uses a native `ubuntu-24.04-arm` runner instead (no Docker/QEMU).
[target.aarch64-unknown-linux-gnu]
pre-build = ["apt-get update && apt-get install -y protobuf-compiler"]
